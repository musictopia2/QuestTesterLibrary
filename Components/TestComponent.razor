<MenuComponent Title="@Title"
               HasBack="HasBack"
               BackClicked="BackClicked"
               >
    @if (DataContext.CivilizationChosen is null)
    {
        <ChooseCivComponent ChoseCiv="ChoseCivAsync" />
        return;
    }
    @ChildContent
</MenuComponent>